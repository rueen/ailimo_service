# 订单号和来源字段实施总结

## ✅ 已完成的工作

### 1. 工具函数和常量
- ✅ 创建 `utils/orderSn.js` - 订单号生成工具
- ✅ 创建 `utils/constants.js` - 订单来源常量定义
- ✅ 更新 `utils/index.js` - 导出新工具

### 2. 数据库模型
已为所有5个订单模型添加新字段：
- ✅ `models/EquipmentReservation.js` - 设备租赁订单
- ✅ `models/CageReservation.js` - 笼位租赁订单
- ✅ `models/ExperimentOperation.js` - 实验代操作订单
- ✅ `models/AnimalOrder.js` - 动物订购订单
- ✅ `models/ReagentOrder.js` - 试剂耗材订单

新增字段：
```javascript
order_sn: {
  type: DataTypes.STRING(20),
  allowNull: false,
  unique: true,
  comment: '订单号'
},
source: {
  type: DataTypes.TINYINT,
  allowNull: false,
  defaultValue: 0,
  comment: '订单来源：0-用户创建 1-管理员创建'
},
created_by_admin_id: {
  type: DataTypes.INTEGER.UNSIGNED,
  comment: '创建管理员ID（仅管理员创建时有值）'
}
```

### 3. 数据库表结构文档
- ✅ 更新 `docs/数据库表结构设计.md` - 所有5个订单表的SQL定义

### 4. 服务层（部分完成）
- ✅ `services/admin/equipmentService.js` - createReservation 函数已修改
- ✅ `services/admin/cageService.js` - createReservation 函数已修改（数据部分完成，签名已完成）

---

## ⚠️ 需要继续完成的工作

### 1. 管理端服务文件

#### experimentService.js
需要修改 `createOperation` 函数：

```javascript
// 在函数开始处添加
const { generateOrderSn, ORDER_PREFIX } = require('../../utils/orderSn');
const { ORDER_SOURCE } = require('../../utils/constants');
const transaction = await db.sequelize.transaction();

// 在数据库操作前添加
const orderSn = await generateOrderSn(ORDER_PREFIX.EXPERIMENT, transaction);
data.order_sn = orderSn;
data.source = adminId ? ORDER_SOURCE.ADMIN : ORDER_SOURCE.USER;
data.created_by_admin_id = adminId || null;

// 修改函数签名
const createOperation = async (data, adminId = null) => {

// 使用事务创建
const operation = await db.ExperimentOperation.create(data, { transaction });
await transaction.commit();

// 更新日志
logger.info(`Experiment operation created: id=${operation.id}, sn=${orderSn}, source=${data.source}`);
```

#### animalOrderService.js
需要修改 `createOrder` 函数（类似修改）：
```javascript
const orderSn = await generateOrderSn(ORDER_PREFIX.ANIMAL, transaction);
data.order_sn = orderSn;
data.source = adminId ? ORDER_SOURCE.ADMIN : ORDER_SOURCE.USER;
data.created_by_admin_id = adminId || null;
```

#### reagentOrderService.js
需要修改 `createOrder` 函数（类似修改）：
```javascript
const orderSn = await generateOrderSn(ORDER_PREFIX.REAGENT, transaction);
data.order_sn = orderSn;
data.source = adminId ? ORDER_SOURCE.ADMIN : ORDER_SOURCE.USER;
data.created_by_admin_id = adminId || null;
```

### 2. 用户端服务文件

#### h5/orderService.js
需要修改以下函数（所有用户端创建的订单 source 都为 0）：

1. **createEquipmentReservation** - 设备租赁
```javascript
const { generateOrderSn, ORDER_PREFIX } = require('../utils/orderSn');
const { ORDER_SOURCE } = require('../utils/constants');

const orderSn = await generateOrderSn(ORDER_PREFIX.EQUIPMENT, transaction);
data.order_sn = orderSn;
data.source = ORDER_SOURCE.USER;
data.created_by_admin_id = null;
```

2. **createCageReservation** - 笼位租赁
```javascript
const orderSn = await generateOrderSn(ORDER_PREFIX.CAGE, transaction);
data.order_sn = orderSn;
data.source = ORDER_SOURCE.USER;
data.created_by_admin_id = null;
```

3. **createExperimentOperation** - 实验代操作
```javascript
const orderSn = await generateOrderSn(ORDER_PREFIX.EXPERIMENT, transaction);
data.order_sn = orderSn;
data.source = ORDER_SOURCE.USER;
data.created_by_admin_id = null;
```

4. **createAnimalOrder** - 动物订购
```javascript
const orderSn = await generateOrderSn(ORDER_PREFIX.ANIMAL, transaction);
data.order_sn = orderSn;
data.source = ORDER_SOURCE.USER;
data.created_by_admin_id = null;
```

5. **createReagentOrder** - 试剂耗材
```javascript
const orderSn = await generateOrderSn(ORDER_PREFIX.REAGENT, transaction);
data.order_sn = orderSn;
data.source = ORDER_SOURCE.USER;
data.created_by_admin_id = null;
```

### 3. 初始化脚本

#### database/init.sql
由于用户会删除并重建数据库，不需要处理老数据迁移。但如果 init.sql 中有订单示例数据，需要为这些数据添加订单号。

如果没有示例数据，可以跳过此步骤。

---

## 📝 实施步骤建议

1. **手动完成剩余服务文件修改**（上述3个管理端 + 5个用户端函数）
2. **删除现有数据库**
   ```bash
   mysql -u root -p -e "DROP DATABASE IF EXISTS ailimo;"
   mysql -u root -p -e "CREATE DATABASE ailimo CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
   ```

3. **使用 Sequelize 同步创建表结构**
   ```bash
   node database/sync.js
   ```

4. **导入初始数据**（如果需要）
   ```bash
   mysql -u root -p ailimo < database/init.sql
   ```

5. **测试验证**
   - 测试用户端创建订单（source=0）
   - 测试管理端创建订单（source=1, created_by_admin_id 有值）
   - 验证订单号格式正确（如 EQ202601160001）
   - 验证订单号唯一性

---

## 📊 新字段说明

### order_sn (订单号)
- 格式：`[前缀][年月日][4位序号]`
- 示例：
  - `EQ202601160001` - 设备租赁第1单
  - `CG202601160023` - 笼位租赁第23单
  - `EX202601170001` - 实验代操作第1单（新一天重新计数）

### source (订单来源)
- `0` - 用户创建（用户在H5端自己下单）
- `1` - 管理员创建（管理员在管理端代用户下单）

### created_by_admin_id (创建管理员ID)
- 仅当 `source=1` 时有值
- 记录是哪个管理员创建的订单
- `source=0` 时为 `null`

---

## 🔍 注意事项

1. **事务处理**：订单号生成必须在事务内，防止并发重复
2. **参数传递**：管理端创建订单时需要传入 `adminId` 参数
3. **H5端简化**：用户端创建订单不需要 `adminId` 参数，固定 `source=0`
4. **日志记录**：建议在创建日志中包含 `order_sn` 和 `source`
5. **接口返回**：订单列表和详情接口应返回 `order_sn`、`source`、`created_by_admin_id`

---

## 测试用例

### 用户端创建订单
```javascript
// 预期结果
{
  id: 1,
  order_sn: 'EQ202601160001',
  source: 0,
  created_by_admin_id: null,
  user_id: 123,
  // ... 其他字段
}
```

### 管理端创建订单
```javascript
// 调用时传入 adminId
await createReservation(data, adminId);

// 预期结果
{
  id: 2,
  order_sn: 'EQ202601160002',
  source: 1,
  created_by_admin_id: 5,  // 管理员ID
  user_id: 123,            // 归属用户ID
  // ... 其他字段
}
```

---

## 完成检查清单

- [ ] experimentService.js - createOperation 修改完成
- [ ] animalOrderService.js - createOrder 修改完成
- [ ] reagentOrderService.js - createOrder 修改完成
- [ ] h5/orderService.js - createEquipmentReservation 修改完成
- [ ] h5/orderService.js - createCageReservation 修改完成
- [ ] h5/orderService.js - createExperimentOperation 修改完成
- [ ] h5/orderService.js - createAnimalOrder 修改完成
- [ ] h5/orderService.js - createReagentOrder 修改完成
- [ ] init.sql 检查并更新（如有示例数据）
- [ ] 数据库重建测试
- [ ] 功能测试通过
