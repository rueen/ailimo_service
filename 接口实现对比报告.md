# 接口实现对比报告

> 对比时间：2026-01-09
> 对比范围：所有接口设计文档 vs 代码实现（routes/admin.js, routes/h5.js）

---

## 一、未实现的接口（文档中有，代码中没有）

### 1. 统计接口（6个）⚠️

- `GET /api/support/statistics/overview` - 综合统计数据
- `GET /api/support/equipment-reservations/statistics` - 设备租赁统计
- `GET /api/support/cage-reservations/statistics` - 笼位租赁统计
- `GET /api/support/experiment-operations/statistics` - 实验代操作统计
- `GET /api/support/animal-orders/statistics` - 动物订购统计
- `GET /api/support/reagent-orders/statistics` - 试剂耗材订购统计

**说明**：统计功能可根据实际需求后期添加。已在文档中标注为"后期实现"。

---

### 2. 详情接口（10个，非必需）

**管理端**：
- `GET /api/support/organizations/:id` - 组织机构详情
- `GET /api/support/research-groups/:id` - 课题组详情
- `GET /api/support/handlers/:id` - 负责人详情
- `GET /api/support/environment-types/:id` - 环境类型详情
- `GET /api/support/animal-types/:id` - 动物类型详情
- `GET /api/support/cage-purposes/:id` - 笼位用途详情
- `GET /api/support/operation-contents/:id` - 操作内容详情
- `GET /api/support/equipment-time-slots/:id` - 设备时间段详情
- `GET /api/support/cage-time-slots/:id` - 笼位时间段详情
- `GET /api/support/experiment-time-slots/:id` - 实验时间段详情

**说明**：这些详情接口可通过列表接口获取完整信息，非必需。

---

### 3. 其他未实现接口（3个）

**管理端**：
- `GET /api/support/cages/available-quantity` - 查询笼位可用数量

**用户端**：
- `GET /api/h5/equipment/:id` - 设备详情（可通过列表接口获取）
- `GET /api/h5/cages/available-quantity` - 查询笼位可用数量

**说明**：这些接口可通过其他接口替代，非必需。

---

## 二、接口设计不一致（文档 vs 代码）

### 可用性查询接口路径不一致

**文档设计**：
- `GET /api/support/equipment/:id/available-slots` - 设备可用时间段（路径参数）

**代码实现**：
- `GET /api/support/equipment-available-slots` - 设备可用时间段（查询参数equipmentId）

**说明**：代码实现使用查询参数而非路径参数。文档中已说明使用查询参数方式，与代码实现一致。

---

## 三、总结

### 未实现接口统计

| 模块 | 接口数量 | 说明 |
|------|---------|------|
| 统计接口 | 6个 | 各模块订单统计，已标注"后期实现" |
| 详情接口 | 10个 | 可通过列表接口获取，非必需 |
| 其他接口 | 3个 | 可通过其他接口替代，非必需 |
| **合计** | **19个** | 均为非核心功能 |

### 不一致接口统计

| 不一致类型 | 涉及接口数量 | 说明 |
|-----------|-------------|------|
| 可用性查询路径 | 1个 | 文档已说明使用查询参数方式，与代码一致 |

**结论**：所有核心功能接口已完整实现，未实现的接口均为非核心功能（19个），可根据需求后期添加。
