# 前端调整指南 - 学院层级新增（H5端/用户端）

## 📋 调整概述

本次调整在组织架构中新增了"学院"层级，层级关系调整为：
- **原来**：会员 > 所属课题组 > 所属组织机构
- **现在**：会员 > 所属课题组 > 所属学院 > 所属组织机构

## 🔄 影响范围

### 涉及功能
1. ✅ 用户注册
2. ✅ 用户登录
3. ✅ 个人信息查询
4. ✅ 组织架构查询（级联选择）

---

## 1️⃣ 用户注册接口调整（重要）

### 1.1 注册接口

**接口地址**: `POST /api/h5/auth/register`

#### 变更内容

**请求参数变化**:
```json
{
  "name": "张三",
  "phone": "13800138000",
  "code": "123456",
  
  // 原来
  "organization_id": 1,      // 必填
  "research_group_id": 1,     // 必填
  
  // 现在
  "organization_id": 1,                       // 可选，组织机构ID（选择"其他"时传null）
  "department_id": 1,                         // 新增：可选，学院ID（选择"其他"时传null）
  "research_group_id": 1,                     // 可选，课题组ID（选择"其他"时传null）
  "user_input_organization_name": "其他大学",  // 新增：可选，用户输入的组织机构名称
  "user_input_department_name": "其他学院",    // 新增：可选，用户输入的学院名称
  "user_input_research_group_name": "其他课题组", // 新增：可选，用户输入的课题组名称
  
  "province_id": 330000,
  "city_id": 33010000,
  "district_id": 330102,
  "address": "文三路XXX号"
}
```

**响应数据变化**:
```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "user": {
      "id": 10,
      "userNo": "U202601160001",
      "name": "张三",
      "phone": "13800138000",
      "organization": { "id": 1, "name": "浙江大学" },
      "department": { "id": 1, "name": "生命科学学院" },  // 新增
      "research_group": { "id": 1, "name": "生物医学研究组" },
      "status": 1,
      "audit_status": 0,
      "reject_reason": null
    }
  }
}
```

---

### 1.2 注册交互逻辑（重要）

#### 交互流程

```
步骤1：选择组织机构
  ├─ 有需要的选项 → 直接选择，organization_id 有值
  └─ 没有需要的选项 → 选择"其他"，弹出输入框输入组织机构名称
      ↓
步骤2：选择学院
  ├─ 有需要的选项 → 直接选择，department_id 有值
  └─ 没有需要的选项 → 选择"其他"，弹出输入框输入学院名称
      ↓
步骤3：选择课题组
  ├─ 有需要的选项 → 直接选择，research_group_id 有值
  └─ 没有需要的选项 → 选择"其他"，弹出输入框输入课题组名称
```

#### 字段逻辑

| 选择情况 | 传参方式 |
|---------|---------|
| 选择了具体的组织机构 | `organization_id` 传ID，`user_input_organization_name` 传 `null` |
| 选择"其他"并手动输入 | `organization_id` 传 `null`，`user_input_organization_name` 传输入的内容 |
| 选择了具体的学院 | `department_id` 传ID，`user_input_department_name` 传 `null` |
| 选择"其他"并手动输入 | `department_id` 传 `null`，`user_input_department_name` 传输入的内容 |
| 选择了具体的课题组 | `research_group_id` 传ID，`user_input_research_group_name` 传 `null` |
| 选择"其他"并手动输入 | `research_group_id` 传 `null`，`user_input_research_group_name` 传输入的内容 |

#### 验证规则

1. `organization_id` 和 `user_input_organization_name` **必须有一个有值**
2. `department_id` 和 `user_input_department_name` **必须有一个有值**
3. `research_group_id` 和 `user_input_research_group_name` **必须有一个有值**
4. 如果选择了具体ID，对应的用户输入字段应该为 `null`

---

### 1.3 注册页面实现要点

#### 表单字段
- 组织机构选择器（必填）
- 学院选择器（必填，未选择/输入组织机构时禁用）
- 课题组选择器（必填，未选择/输入学院时禁用）

#### 选择器功能
每个选择器都需要：
1. 显示选项列表（从接口获取）
2. 在列表末尾添加"其他"选项
3. 选择"其他"时弹出输入框
4. 输入框需要验证：不能为空，不能只包含空格，建议限制100字符

#### 级联逻辑

**选择组织机构（具体ID）：**
1. 设置 `organization_id` 为选择的ID，`user_input_organization_name` 为 `null`
2. 清空学院和课题组的所有选择和列表
3. 调用 `GET /api/h5/departments?organization_id=xxx` 加载学院列表

**选择组织机构（其他）：**
1. 弹出输入框，验证后设置 `user_input_organization_name`，`organization_id` 为 `null`
2. 清空学院和课题组的所有选择和列表
3. 自动打开学院选择器（此时列表为空，只有"其他"选项）

**选择学院（具体ID）：**
1. 设置 `department_id` 为选择的ID，`user_input_department_name` 为 `null`
2. 清空课题组的所有选择和列表
3. 调用 `GET /api/h5/research-groups?department_id=xxx` 加载课题组列表

**选择学院（其他）：**
1. 弹出输入框，验证后设置 `user_input_department_name`，`department_id` 为 `null`
2. 清空课题组的所有选择和列表
3. 自动打开课题组选择器（此时列表为空，只有"其他"选项）

**选择课题组（具体ID/其他）：**
同上逻辑

#### 提交验证
- `organization_id` 和 `user_input_organization_name` 必须有一个有值
- `department_id` 和 `user_input_department_name` 必须有一个有值
- `research_group_id` 和 `user_input_research_group_name` 必须有一个有值

---

## 2️⃣ 用户登录接口调整

### 2.1 登录接口

**接口地址**: `POST /api/h5/auth/login`

#### 变更内容

**请求参数**: 无变化

**响应数据变化**:
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "user": {
      "id": 10,
      "userNo": "U202601160001",
      "name": "张三",
      "phone": "13800138000",
      "organization": { "id": 1, "name": "浙江大学" },
      "department": { "id": 1, "name": "生命科学学院" },  // 新增
      "research_group": { "id": 1, "name": "生物医学研究组" },
      "status": 1,
      "audit_status": 1,
      "reject_reason": null
    }
  }
}
```

#### 前端调整建议
- 保存用户信息时，需要保存 `department` 字段
- 如果需要展示用户的学院信息，可以使用 `user.department.name`

---

## 3️⃣ 个人信息接口调整

### 3.1 获取个人信息

**接口地址**: `GET /api/h5/auth/profile`

#### 变更内容

**响应数据变化**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 10,
    "user_no": "U202601160001",
    "name": "张三",
    "phone": "13800138000",
    "province_id": 330000,
    "city_id": 33010000,
    "district_id": 330102,
    "province": { "id": 330000, "name": "浙江省", "code": "330000" },
    "city": { "id": 33010000, "name": "杭州市", "code": "330100" },
    "district": { "id": 330102, "name": "西湖区", "code": "330102" },
    "address": "文三路XXX号",
    "organization": { "id": 1, "name": "浙江大学" },
    "department": { "id": 1, "name": "生命科学学院" },  // 新增
    "research_group": { "id": 1, "name": "生物医学研究组" },
    "status": 1,
    "audit_status": 1,
    "created_at": "2026-01-01 09:00:00",
    "updated_at": "2026-01-05 10:00:00"
  }
}
```

#### 前端调整建议
- 个人中心页面展示学院信息
- 信息展示顺序建议：组织机构 → 学院 → 课题组

---

## 4️⃣ 组织架构查询接口

### 4.1 获取学院列表（新增）

**接口地址**: `GET /api/h5/departments`

**请求参数**:
```json
{
  "organization_id": 1  // 必填，组织机构ID
}
```

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "生命科学学院",
      "organization_id": 1
    },
    {
      "id": 2,
      "name": "医学院",
      "organization_id": 1
    }
  ]
}
```

#### 使用场景
用于注册时的级联选择：选择组织机构后，根据 `organization_id` 查询学院列表

---

### 4.2 获取课题组列表（接口调整）

**接口地址**: `GET /api/h5/research-groups`

#### 变更内容

**请求参数变化**:
```json
{
  // 原来
  "organization_id": 1  // 已废弃

  // 现在
  "department_id": 1    // 必填，学院ID
}
```

**响应数据变化**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "生物医学研究组",
      // 原来
      "organization_id": 1  // 已移除
      
      // 现在
      "department_id": 1    // 新增
    }
  ]
}
```

#### 使用场景
用于注册时的级联选择：选择学院后，根据 `department_id` 查询课题组列表

---

## 5️⃣ 级联选择完整流程图

```
开始注册
    ↓
调用 GET /api/h5/organizations
获取组织机构列表（包含"其他"选项）
    ↓
用户选择组织机构
    ├─ 选择具体组织机构 → organization_id 有值
    │       ↓
    │   调用 GET /api/h5/departments?organization_id=xxx
    │   获取学院列表（包含"其他"选项）
    │       ↓
    │   用户选择学院
    │       ├─ 选择具体学院 → department_id 有值
    │       │       ↓
    │       │   调用 GET /api/h5/research-groups?department_id=xxx
    │       │   获取课题组列表（包含"其他"选项）
    │       │       ↓
    │       │   用户选择课题组
    │       │       ├─ 选择具体课题组 → research_group_id 有值
    │       │       └─ 选择"其他" → 手动输入 → user_input_research_group_name 有值
    │       │
    │       └─ 选择"其他" → 手动输入 → user_input_department_name 有值
    │               ↓
    │           显示课题组选择（只有"其他"选项）
    │               ↓
    │           用户选择"其他" → 手动输入 → user_input_research_group_name 有值
    │
    └─ 选择"其他" → 手动输入 → user_input_organization_name 有值
            ↓
        显示学院选择（只有"其他"选项）
            ↓
        用户选择"其他" → 手动输入 → user_input_department_name 有值
            ↓
        显示课题组选择（只有"其他"选项）
            ↓
        用户选择"其他" → 手动输入 → user_input_research_group_name 有值
            ↓
        提交注册
```

---

## 6️⃣ API 接口汇总

### 新增接口
- `GET /api/h5/departments` - 根据组织机构获取学院列表

### 修改接口
- `POST /api/h5/auth/register` - 新增 `department_id` 和三个用户输入字段
- `POST /api/h5/auth/login` - 返回数据新增 `department` 字段
- `GET /api/h5/auth/profile` - 返回数据新增 `department` 字段
- `GET /api/h5/research-groups` - 参数从 `organization_id` 改为 `department_id`

---

## 7️⃣ 注意事项

### 必须遵守的规则
1. **级联逻辑**：必须按照 组织机构 → 学院 → 课题组 的顺序选择
2. **"其他"选项**：每个层级都必须提供"其他"选项供用户手动输入
3. **互斥规则**：ID字段和用户输入字段是互斥的，只能有一个有值
4. **验证规则**：每个层级都必须有值（要么是ID，要么是用户输入）
5. **清空逻辑**：上级改变时，必须清空所有下级的选择

### UI/UX建议
1. 未选择上级时，下级选择器应该禁用或显示提示
2. "其他"选项建议放在列表最后
3. 输入框弹窗应该有字数限制（建议100字符）
4. 输入验证：不能为空，不能只包含空格
5. 成功提示：选择或输入后应该有明确的视觉反馈

---

## 8️⃣ 测试要点

### 功能测试
- ✅ 正常流程：依次选择组织机构、学院、课题组
- ✅ "其他"流程：依次选择"其他"并手动输入
- ✅ 混合流程：部分选择具体选项，部分选择"其他"
- ✅ 级联联动：上级改变时，下级是否正确清空
- ✅ 数据提交：参数是否按照规则正确传递

### 边界测试
- ✅ 未选择上级时，下级是否正确禁用
- ✅ 输入为空时的验证提示
- ✅ 输入只包含空格时的验证
- ✅ 超长输入的处理
- ✅ 网络错误时的友好提示

### 兼容性测试
- ✅ iOS系统测试
- ✅ Android系统测试
- ✅ 不同屏幕尺寸适配
- ✅ 弱网环境测试

---

## 9️⃣ 常见问题

### Q1: 选择"其他"后，如何触发学院选择？
**A**: 在"其他"输入确认后，自动打开学院选择器。如果组织机构选择了"其他"，学院选择器中只会显示"其他"选项。

### Q2: 用户输入的内容会保存到哪里？
**A**: 用户输入的内容会保存到数据库的 `user_input_xxx_name` 字段中，供管理员审核时参考。管理员可以根据输入内容创建对应的组织机构/学院/课题组，或驳回申请。

### Q3: 如果用户选择了具体的ID，用户输入字段是否需要传 `null`？
**A**: 是的，必须显式传 `null`，不要省略该字段。

### Q4: 级联选择报错"找不到数据"？
**A**: 检查接口参数是否正确：
- 学院列表接口：`organization_id` 参数
- 课题组列表接口：`department_id` 参数（不是 `organization_id`）

---

## 🔟 UI界面参考

### 注册页面布局建议

```
┌─────────────────────────────┐
│  用户注册                    │
├─────────────────────────────┤
│  姓名        [张三________]  │
│  手机号      [13800138000_]  │
│  验证码      [123456_] [获取] │
│  ─────────────────────────  │
│  组织机构    [浙江大学_____▼] │
│  学院        [生命科学学院__▼] │
│  课题组      [生物医学研究组▼] │
│  ─────────────────────────  │
│  省份        [浙江省_______▼] │
│  城市        [杭州市_______▼] │
│  区县        [西湖区_______▼] │
│  详细地址    [文三路XXX号___]  │
│  ─────────────────────────  │
│         [注册]               │
└─────────────────────────────┘
```

### 选择器示例

```
┌─────────────────────────────┐
│  选择组织机构                 │
├─────────────────────────────┤
│  浙江大学                    │
│  复旦大学                    │
│  上海交通大学                │
│  ...                        │
│  其他 ⭐                    │
├─────────────────────────────┤
│  [取消]            [确定]    │
└─────────────────────────────┘
```

### 输入弹窗示例

```
┌─────────────────────────────┐
│  请输入组织机构名称           │
├─────────────────────────────┤
│  [___________________]      │
│                             │
├─────────────────────────────┤
│  [取消]            [确定]    │
└─────────────────────────────┘
```

---

## 📞 技术支持

如有疑问，请联系后端开发团队。

---

## 附录：完整数据流示例

### 场景1：全部选择具体选项

```json
// 请求数据
{
  "name": "张三",
  "phone": "13800138000",
  "code": "123456",
  "organization_id": 1,
  "department_id": 1,
  "research_group_id": 1,
  "user_input_organization_name": null,
  "user_input_department_name": null,
  "user_input_research_group_name": null
}
```

### 场景2：全部选择"其他"

```json
// 请求数据
{
  "name": "李四",
  "phone": "13900139000",
  "code": "654321",
  "organization_id": null,
  "department_id": null,
  "research_group_id": null,
  "user_input_organization_name": "某某大学",
  "user_input_department_name": "某某学院",
  "user_input_research_group_name": "某某研究组"
}
```

### 场景3：混合选择

```json
// 请求数据
{
  "name": "王五",
  "phone": "13700137000",
  "code": "111111",
  "organization_id": 1,                         // 选择了具体组织机构
  "department_id": null,                        // 选择了"其他"
  "research_group_id": 5,                       // 选择了具体课题组
  "user_input_organization_name": null,
  "user_input_department_name": "新设立学院",    // 手动输入
  "user_input_research_group_name": null
}
```
